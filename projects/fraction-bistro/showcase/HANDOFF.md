# Fraction Bistro (#02) — Handoff

Project: Fraction Bistro (#02)
Spec: .showcase/02_fraction-bistro.md
Status: Deployed to Shiplo, live-verified at https://fraction-bistro.shiplo.site — ready for registry `live`.
Build: PASS — `npm run build` (vue-tsc -b + vite build) in `projects/fraction-bistro`; dist = 32 files / 529,416 bytes (0.50 MB); `npm run verify:static -- fraction-bistro` PASS; `npm run test:engine` PASS (133 checks over all 16 orders, correct + instructive-wrong paths).
Shiplo deployment: URL: https://fraction-bistro.shiplo.site (site 04265ba4-dc3b-40b2-8e8e-29f09d64c8c6, release 1c84429a-04ed-46bd-93c3-05df94fe5d81, deployment 0973467d-6060-4c47-a853-ea325f43c3ab, activated 2026-09-01T05:00:20.392Z, 32 files / 529,416 bytes — byte-identical to dist). artifactSha256 b671caab9894d7a55087c3ad6852beca982a6b0dba1cf4840d2664c789985e74 (tar -czf - -C dist . | sha256sum).
Live verification: PASS — full order→cut→serve flow driven headless against the public URL at 1440×900, 1024×768, 390×844 (0 console errors each; mouse click + touch tap + keyboard arrows/Enter + synthetic pointer-drag with ghost flight all exercised; build order serve + stamp, wrong-cut nudge + recovery, equivalent order with chef plate + overlay, compare order with wrong-then-right sign, recipe-book entry, progress 4/16, two-step reset); `cdp:smoke` PASS; `check:fonts` PASS on live (Fraunces + Source Sans 3, latin+digits OK, vietnamese OK).
Screenshots: cover.webp 1440×900 (staged serve moment: margherita cut in eighths, five plated, SERVITO stamp), desktop.webp 1440×900 (honest landing), tablet.webp 1024×768 (honest landing — hero viewport), mobile.webp 390×844 (honest landing). All captured from https://fraction-bistro.shiplo.site, 21–64 KB each.
Codex-generated assets: NONE — all visuals are original SVG/CSS authored in-project (dishes rendered data-driven from dishes.json); paper grain is a procedural SVG feTurbulence file (spec-endorsed procedural texture, no raster).
Third-party assets: vue 3.5.42 (MIT) · gsap 3.15.0 (GreenSock Standard "No Charge" License — recorded correctly, NOT MIT) · Fraunces (OFL-1.1, Undercase Type) via @fontsource/fraunces 5.3.0 (MIT pkg) · Source Sans 3 (OFL-1.1, Adobe) via @fontsource/source-sans-3 5.3.0 (MIT pkg). Dev-only: vite 7.3.6, @vitejs/plugin-vue 6.0.8, typescript 5.9.3, vue-tsc 3.3.11. Full table in THIRD_PARTY_NOTICES.md.
License/provenance: PASS — LICENSE/NOTICE present (Apache-2.0); SPDX headers on all src/scripts authored files (none on JSON/generated/third-party); package.json `private: true` + `license: Apache-2.0`; no secrets; localStorage anonymous-only (served order ids) with two-step reset; deployment.json status "verified" with verbatim URL + real timestamps; README states demo/reference notice + "art-directed and maintained by Shiplo HQ with AI-assisted implementation".
Security: PASS — `npm audit` 0 vulnerabilities; no eval/dynamic execution; no runtime network calls beyond the static bundle (verify:static enforces; only external refs are none); `npm run check:repo` PASS.
Impeccable: dual-agent critique (design review 33/40 + detector with browser injection) → all findings fixed or dispositioned in design/DESIGN_DECISIONS.md §15b. Fixed: P1 silent work destruction (per-order session store so back-nav keeps cuts, re-cut announces returned slices, two-step "Clear all stamps?" reset), P1 focus never moved between screens (now focuses each screen heading), P1 plate slice targets ~30px + missing Return control (wedge hit areas widened + "Return last slice" button), board column imbalance 210%/42% (3-column rail ≥1200px + sticky menu column + compact tickets), compare sign scaffold (overlay auto-opens after a wrong sign with guiding caption), target fraction scrolling away on mobile (sticky mini-ticket, request as h3), peak-end "Next order" continuation, side-tab 6px border on feedback (coordinator-relayed — replaced with full 1.5px status hairline + icon + text), em-dash overuse 11→~2, ink-soft micro-labels → ink, stamp occlusion, duplicate sr-only text, Math.random uid → useId, mixed curly/straight quotes, bilingual title. Accepted-by-brief: cream palette (locked spec), Fraunces-heavy board (serif dish names = menu thesis), masthead overline tracked caps (menu-cover convention, shortened to 20 chars), SVG dish "shape-assembled" advisory (original SVG dishes are required by spec). Snapshot: .impeccable/critique/2026-09-01T04-48-54Z__projects-fraction-bistro.md.
Remaining issues: NONE blocking. Known accepted limits (documented, not defects): drag on small screens is lower-precision — tap and keyboard are the primary paths (metadata note); woff+woff2 font duplication (325 KB fonts in dist) kept for fallback coverage, total 0.50 MB far under budget; drag path automated-tested via synthetic PointerEvents (CDP Input domain does not synthesize pointermove) — tap/keyboard tested with native CDP input.
Root integration required:
- showcase.json: set #02 fraction-bistro `status: "live"`, `demo.url: "https://fraction-bistro.shiplo.site"`, `demo.deployedAt: "2026-09-01T05:00:20Z"`, screenshots paths showcase/{cover,desktop,tablet,mobile}.webp (metadata.json already mirrors these).
- README gallery: via npm run gallery
- sourceCommitSha finalization: YES
